import React from 'react';
import { PickerView, PickerViewColumn, View } from 'rsmax';

export default () => (
  <PickerView>
    <PickerViewColumn>
      <View></View>123
    </PickerViewColumn>
  </PickerView>
);
