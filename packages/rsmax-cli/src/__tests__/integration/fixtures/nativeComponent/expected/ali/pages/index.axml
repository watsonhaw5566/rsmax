<template is="REMAX_TPL" data="{{ root: root }}" />

<template name="REMAX_TPL">
  <block a:for="{{root.children}}" a:key="{{item.id}}">
    <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
  </block>
</template>

<template name="REMAX_TPL_1_a-index-184b57f">
  <a-index-184b57f
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </a-index-184b57f>
</template>

<template name="REMAX_TPL_1_a-index-20116ca">
  <a-index-20116ca
    foo="{{item.props['foo']}}"
    title="{{item.props['title']}}"
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </a-index-20116ca>
</template>

<template name="REMAX_TPL_1_b-index-1621a32">
  <b-index-1621a32
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </b-index-1621a32>
</template>

<template name="REMAX_TPL_1_c-index-92afd3b">
  <c-index-92afd3b
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </c-index-92afd3b>
</template>

<template name="REMAX_TPL_1_commonjs-index-ecf6ba2">
  <commonjs-index-ecf6ba2
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </commonjs-index-ecf6ba2>
</template>

<template name="REMAX_TPL_1_complex-index-818a683">
  <complex-index-818a683
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </complex-index-818a683>
</template>

<template name="REMAX_TPL_1_d-index-6a5e7c1">
  <d-index-6a5e7c1
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </d-index-6a5e7c1>
</template>

<template name="REMAX_TPL_1_e-index-92b6247">
  <e-index-92b6247
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </e-index-92b6247>
</template>

<template name="REMAX_TPL_1_my-plugin-dddd-5ebbbd9">
  <my-plugin-dddd-5ebbbd9
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </my-plugin-dddd-5ebbbd9>
</template>

<template name="REMAX_TPL_1_my-plugin-xx-f094b58">
  <my-plugin-xx-f094b58
    foo="{{item.props['foo']}}"
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </my-plugin-xx-f094b58>
</template>

<template name="REMAX_TPL_1_not-in-jsx-index-69b1359">
  <not-in-jsx-index-69b1359
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </not-in-jsx-index-69b1359>
</template>

<template name="REMAX_TPL_1_slot-index-e5a43a8">
  <slot-index-e5a43a8
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </slot-index-e5a43a8>
</template>

<template name="REMAX_TPL_1_src-index-58e3765">
  <src-index-58e3765
    ref="{{item.props['__ref']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view
          slot="{{item.props['slot']}}"
          class="{{item.props['class']}}"
          id="{{item.props['id']}}"
        >
          <block a:for="{{item.children}}" key="{{item.id}}">
            <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
          </block>
        </view>
      </block>
      <block a:else>
        <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
      </block>
    </block>
  </src-index-58e3765>
</template>

<template name="REMAX_TPL_1_view">
  <view
    animation="{{item.props['animation']}}"
    class="{{item.props['class']}}"
    disable-scroll="{{item.props['disable-scroll']}}"
    hidden="{{item.props['hidden']}}"
    hover-class="{{item.props['hover-class']}}"
    hover-start-time="{{item.props['hover-start-time']}}"
    hover-stay-time="{{item.props['hover-stay-time']}}"
    hover-stop-propagation="{{item.props['hover-stop-propagation']}}"
    id="{{item.props['id']}}"
    onAnimationEnd="{{item.props['onAnimationEnd']}}"
    onAnimationIteration="{{item.props['onAnimationIteration']}}"
    onAnimationStart="{{item.props['onAnimationStart']}}"
    onAppear="{{item.props['onAppear']}}"
    onDisappear="{{item.props['onDisappear']}}"
    onFirstAppear="{{item.props['onFirstAppear']}}"
    onLongTap="{{item.props['onLongTap']}}"
    onTap="{{item.props['onTap']}}"
    onTouchCancel="{{item.props['onTouchCancel']}}"
    onTouchEnd="{{item.props['onTouchEnd']}}"
    onTouchMove="{{item.props['onTouchMove']}}"
    onTouchStart="{{item.props['onTouchStart']}}"
    onTransitionEnd="{{item.props['onTransitionEnd']}}"
    style="{{item.props['style']}}"
  >
    <block a:for="{{item.children}}" key="{{item.id}}">
      <template is="REMAX_TPL_1_CONTAINER" data="{{item: item}}" />
    </block>
  </view>
</template>

<template name="REMAX_TPL_1_plain-text">
  <block>{{item.text}}</block>
</template>

<template name="REMAX_TPL_1_CONTAINER">
  <template is="{{'REMAX_TPL_1_' + item.type}}" data="{{item: item}}" />
</template>

<template is="REMAX_TPL" data="{{ root: modalRoot }}" />
